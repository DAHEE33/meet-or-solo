# 데이터베이스 설계

이 문서는 9-1단계 실제 서비스 DB 테이블 설계 검토/확정 결과를 기록합니다.

이번 단계에서는 Flyway migration SQL을 생성하지 않습니다. 이미 적용된 `db/migration/V1__init.sql`은 수정하지 않으며, 다음 9-2단계에서 이 문서를 기준으로 `V2` 이후 migration을 새로 추가합니다.

## 1. 설계 범위

대상 DB는 PostgreSQL입니다.

설계 대상은 MVP에서 필요한 실제 서비스 테이블입니다.

- 회원/인증
- 약관 동의
- 관광공사 축제/장소 캐시
- GPS 체크인 결과
- PostgreSQL 기반 매칭 상태
- 제한형 상태 동기화 이벤트
- 패널티/쿨다운
- 차단/신고/평가/관리자 조치
- 솔로 코스와 추천 클릭 로그

이번 단계에서 하지 않는 작업:

- Flyway SQL 파일 생성
- `db/migration/V2~` 파일 생성
- `V1__init.sql` 수정
- backend/frontend 코드 수정
- nginx, docker-compose, GitHub Actions 수정
- 실제 DB migration 적용
- 실제 Oracle VM 접속

## 2. 공통 설계 원칙

- PK는 PostgreSQL `BIGINT GENERATED BY DEFAULT AS IDENTITY`를 기본 후보로 둔다.
- 상태값은 PostgreSQL `ENUM`보다 `VARCHAR` + `CHECK constraint`를 우선 사용한다.
- 모든 주요 업무 테이블은 `created_at`, `updated_at`을 기본 후보로 둔다.
- 이력성 테이블은 `updated_at` 없이 append-only로 둘 수 있다.
- soft delete가 필요한 테이블은 `deleted_at` 또는 `status`를 사용한다.
- FK는 도메인상 삭제 정책을 검토해 `CASCADE`를 남용하지 않는다.
- 운영 데이터와 개인정보는 분리 사고한다.
- 원본 GPS 좌표는 저장하지 않는다.
- 자유 채팅 테이블은 만들지 않는다.
- Redis 없이 PostgreSQL의 `status`, `expires_at`, `locked_at`, transaction lock, partial unique index로 상태를 관리한다.
- 관광공사 OpenAPI 원본 응답은 감사와 캐시 보강이 필요한 테이블에 한해 `raw_data JSONB`로 저장할 수 있다.

## 3. 테이블 분류

### MVP 필수 테이블

| 영역 | 테이블 |
| --- | --- |
| 회원/인증 | `members`, `member_consents`, `refresh_tokens` |
| 관광/축제 | `festivals`, `festival_images`, `tour_places`, `tour_api_call_logs` |
| 체크인 | `festival_checkins` |
| 매칭 | `user_blocks`, `match_pools`, `match_attempts`, `match_attempt_members`, `match_proposals`, `match_responses`, `match_groups`, `match_group_members`, `match_events`, `match_cooldowns`, `match_penalty_events` |
| 안전/운영 | `member_reviews`, `reports`, `admin_actions` |
| 추천/솔로 | `solo_courses`, `solo_course_places`, `recommendation_click_logs` |

### 추후 분리 가능 테이블

| 후보 | 판단 |
| --- | --- |
| `member_roles` | MVP는 `members.role`로 충분하다. 권한이 복잡해지면 분리한다. |
| `member_profile_images` | MVP는 `members.profile_image_url`로 시작한다. 이미지 변경 이력이 필요하면 분리한다. |
| `match_group_cancellations` | MVP는 `match_events`와 `match_group_members.cancelled_at`로 표현한다. 상세 통계가 필요하면 분리한다. |
| `web_push_subscriptions` | Web Push 구현 단계에서 추가한다. 이번 DB 설계에는 테이블 후보만 별도 보류한다. |
| `inquiries` | 1:1 문의 센터 구현 시 추가한다. MVP 안전 기능의 핵심은 `reports`와 `admin_actions`다. |
| `api_batch_runs` | 초기에는 `tour_api_call_logs`로 수동/스케줄 호출 기록을 관리한다. 배치 단위 추적이 필요하면 분리한다. |

### 제외 테이블

| 제외 테이블 | 이유 |
| --- | --- |
| 자유 채팅 메시지 테이블 | 서비스 정책상 자유 채팅을 구현하지 않는다. |
| Redis 상태 테이블 | Redis는 MVP에서 제외한다. |
| 원본 GPS 좌표 저장 테이블 | 위치정보 최소 수집 원칙에 따라 저장하지 않는다. |

## 4. Migration 분리안

### V2__create_core_tables.sql

회원, 인증, 약관, 관광공사 캐시, 체크인처럼 다른 도메인의 기반이 되는 테이블을 생성한다.

- `members`
- `member_consents`
- `refresh_tokens`
- `festivals`
- `festival_images`
- `tour_places`
- `tour_api_call_logs`
- `festival_checkins`

### V3__create_matching_tables.sql

PostgreSQL 기반 매칭 상태와 동시성 방어에 필요한 테이블을 생성한다.

- `user_blocks`
- `match_pools`
- `match_attempts`
- `match_attempt_members`
- `match_proposals`
- `match_responses`
- `match_groups`
- `match_group_members`
- `match_events`
- `match_cooldowns`
- `match_penalty_events`

### V4__create_safety_admin_recommendation_tables.sql

신고/평가/관리자 조치와 추천/솔로 코스 로그를 생성한다.

- `member_reviews`
- `reports`
- `admin_actions`
- `solo_courses`
- `solo_course_places`
- `recommendation_click_logs`

V4 이름은 기존 제안의 `safety-admin`에 추천/솔로 코스가 함께 들어간다는 점을 드러내기 위해 `safety_admin_recommendation`을 권장한다.

## 5. 공통 상태값 후보

### members.status

```text
ACTIVE
PROFILE_REQUIRED
SUSPENDED
WITHDRAWN
DELETED
```

### members.role

```text
USER
ADMIN
```

MVP는 `members.role`로 시작한다. 관리자 권한이 세분화되면 `member_roles`로 분리한다.

### match_pools.status

```text
WAITING
LOCKED
PROPOSED
MATCHED
EXPIRED
CANCELLED
COOLDOWN
```

### match_attempts.status

```text
CREATED
PROPOSING
WAITING_RESPONSES
INSUFFICIENT_MEMBERS
CONFIRMED
FAILED
CANCELLED
EXPIRED
```

### match_proposals.status

```text
SENT
ACCEPTED
REJECTED
TIMEOUT
EXPIRED
```

### match_responses.response

```text
ACCEPTED
REJECTED
TIMEOUT
START_WITH_CURRENT_MEMBERS
CANCEL_CURRENT_MEMBERS
```

### match_groups.status

```text
CONFIRMED
IN_PROGRESS
COMPLETED
CANCELLED
```

### reports.status

```text
SUBMITTED
REVIEWING
RESOLVED
REJECTED
ACTION_TAKEN
```

### admin_actions.action_type

```text
WARNING
SUSPEND
BAN
UNBAN
REPORT_RESOLVE
REPORT_REJECT
MANUAL_PENALTY
DATA_CORRECTION
```

## 6. 테이블별 설계안

### members

| 항목 | 내용 |
| --- | --- |
| 목적 | Kakao OAuth 기반 회원과 프로필, 서비스 상태를 관리한다. |
| 주요 컬럼 | `id`, `provider`, `provider_user_id`, `nickname`, `profile_image_url`, `gender_encrypted`, `age_range_encrypted`, `manner_temperature`, `penalty_score`, `role`, `status`, `last_login_at`, `withdrawn_at`, `created_at`, `updated_at` |
| PK | `id` |
| FK | 없음 |
| 상태값 | `status`, `role` |
| CHECK | `provider IN ('KAKAO')`, `role IN ('USER','ADMIN')`, `status IN (...)`, `manner_temperature BETWEEN 0 AND 100`, `penalty_score >= 0` |
| UNIQUE | `(provider, provider_user_id)` |
| INDEX | `idx_members_status`, `idx_members_role`, `idx_members_created_at` |
| 개인정보/보안 | 성별/연령대는 암호화 저장 전제로 `BYTEA` 후보를 사용한다. 탈퇴 시 닉네임/이미지/민감정보를 익명화한다. |
| MVP 필수 | 필수 |

### member_consents

| 항목 | 내용 |
| --- | --- |
| 목적 | 서비스 약관, 개인정보, 위치정보 동의 이력을 관리한다. |
| 주요 컬럼 | `id`, `member_id`, `consent_type`, `version`, `agreed`, `agreed_at`, `revoked_at`, `created_at` |
| PK | `id` |
| FK | `member_id -> members.id` |
| 상태값 | `consent_type` |
| CHECK | `consent_type IN ('TERMS','PRIVACY','LOCATION','MARKETING')` |
| UNIQUE | `(member_id, consent_type, version)` |
| INDEX | `idx_member_consents_member_id`, `idx_member_consents_type` |
| 개인정보/보안 | 법적 증빙 목적의 최소 이력만 저장한다. |
| MVP 필수 | 필수 |

### refresh_tokens

| 항목 | 내용 |
| --- | --- |
| 목적 | JWT Refresh Token 저장, rotation, 폐기를 관리한다. |
| 주요 컬럼 | `id`, `member_id`, `token_hash`, `expires_at`, `revoked_at`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `member_id -> members.id` |
| 상태값 | `revoked_at` 기반 |
| CHECK | `expires_at > created_at` |
| UNIQUE | `token_hash` |
| INDEX | `idx_refresh_tokens_member_active`, `idx_refresh_tokens_expires_at` |
| 개인정보/보안 | 원문 token을 저장하지 않고 hash만 저장한다. |
| MVP 필수 | 필수 |

### festivals

| 항목 | 내용 |
| --- | --- |
| 목적 | 관광공사 OpenAPI 축제 데이터를 캐시하고 체크인/매칭 기준점으로 사용한다. |
| 주요 컬럼 | `id`, `content_id`, `content_type_id`, `title`, `address`, `area_code`, `sigungu_code`, `event_start_date`, `event_end_date`, `map_x`, `map_y`, `checkin_radius_meters`, `meeting_radius_meters`, `status`, `last_synced_at`, `raw_data`, `created_at`, `updated_at` |
| PK | `id` |
| FK | 없음 |
| 상태값 | `ACTIVE`, `INACTIVE`, `ENDED`, `HIDDEN` |
| CHECK | `content_type_id = '15'`, `checkin_radius_meters > 0`, `meeting_radius_meters > 0` |
| UNIQUE | `content_id` |
| INDEX | `idx_festivals_period`, `idx_festivals_status`, `idx_festivals_area`, `idx_festivals_content_id` |
| 개인정보/보안 | 공공데이터 캐시이며 개인정보 없음. 원천 데이터 임의 수정 여부를 구분할 수 있도록 `raw_data`를 둔다. |
| MVP 필수 | 필수 |

### festival_images

| 항목 | 내용 |
| --- | --- |
| 목적 | 축제 썸네일과 상세 이미지를 캐시한다. |
| 주요 컬럼 | `id`, `festival_id`, `origin_image_url`, `thumbnail_url`, `display_order`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `festival_id -> festivals.id` |
| 상태값 | 없음 |
| CHECK | `display_order >= 0` |
| UNIQUE | `(festival_id, origin_image_url)` |
| INDEX | `idx_festival_images_festival_order` |
| 개인정보/보안 | 공공 이미지 URL만 저장한다. |
| MVP 필수 | 필수 |

### tour_places

| 항목 | 내용 |
| --- | --- |
| 목적 | 주변 관광지/음식점/레포츠/문화시설을 추천과 솔로 코스에 사용하기 위해 캐시한다. |
| 주요 컬럼 | `id`, `content_id`, `content_type_id`, `title`, `address`, `map_x`, `map_y`, `tel`, `image_url`, `status`, `last_synced_at`, `raw_data`, `created_at`, `updated_at` |
| PK | `id` |
| FK | 없음 |
| 상태값 | `ACTIVE`, `INACTIVE`, `HIDDEN` |
| CHECK | `content_type_id IN ('12','14','28','39')` |
| UNIQUE | `content_id` |
| INDEX | `idx_tour_places_type_status`, `idx_tour_places_content_id` |
| 개인정보/보안 | 공공데이터 캐시이며 개인정보 없음. |
| MVP 필수 | 필수 |

### tour_api_call_logs

| 항목 | 내용 |
| --- | --- |
| 목적 | 관광공사 API 호출 증빙과 장애 분석을 위한 로그를 저장한다. |
| 주요 컬럼 | `id`, `operation_name`, `request_key`, `mobile_app`, `status_code`, `success`, `response_time_ms`, `result_count`, `error_message`, `called_at`, `created_at` |
| PK | `id` |
| FK | 없음 |
| 상태값 | 없음 |
| CHECK | `response_time_ms >= 0`, `mobile_app = 'SoloIn'` |
| UNIQUE | 없음 |
| INDEX | `idx_tour_api_call_logs_operation_called_at`, `idx_tour_api_call_logs_success_called_at` |
| 개인정보/보안 | API Key는 저장하지 않는다. 단말 GPS 원본 좌표도 저장하지 않는다. |
| MVP 필수 | 필수 |

### festival_checkins

| 항목 | 내용 |
| --- | --- |
| 목적 | GPS 검증에 성공한 축제 체크인 결과를 저장하고 매칭풀 진입 조건으로 사용한다. |
| 주요 컬럼 | `id`, `member_id`, `festival_id`, `distance_meters`, `status`, `checked_in_at`, `expires_at`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `member_id -> members.id`, `festival_id -> festivals.id` |
| 상태값 | `ACTIVE`, `EXPIRED`, `CANCELLED` |
| CHECK | `distance_meters >= 0`, `expires_at > checked_in_at`, `status IN (...)` |
| UNIQUE | active 상태의 `(member_id, festival_id)` partial unique index 후보 |
| INDEX | `idx_festival_checkins_member_active`, `idx_festival_checkins_festival_active`, `idx_festival_checkins_expires_at` |
| 개인정보/보안 | 원본 latitude/longitude는 저장하지 않는다. 성공 metadata와 거리만 저장한다. |
| MVP 필수 | 필수 |

### user_blocks

| 항목 | 내용 |
| --- | --- |
| 목적 | 차단 관계를 저장하고 매칭 후보에서 양방향 제외한다. |
| 주요 컬럼 | `id`, `blocker_member_id`, `blocked_member_id`, `reason`, `created_at` |
| PK | `id` |
| FK | `blocker_member_id -> members.id`, `blocked_member_id -> members.id` |
| 상태값 | 없음 |
| CHECK | `blocker_member_id <> blocked_member_id` |
| UNIQUE | `(blocker_member_id, blocked_member_id)` |
| INDEX | `idx_user_blocks_blocker`, `idx_user_blocks_blocked` |
| 개인정보/보안 | 차단 사유는 구조화 또는 짧은 내부 값으로 제한한다. |
| MVP 필수 | 필수 |

### match_pools

| 항목 | 내용 |
| --- | --- |
| 목적 | 체크인한 사용자의 매칭 대기 상태와 조건을 저장한다. |
| 주요 컬럼 | `id`, `member_id`, `festival_id`, `checkin_id`, `preferred_group_size`, `allow_minimum_two`, `tags`, `status`, `entered_at`, `search_expires_at`, `locked_at`, `lock_token`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `member_id -> members.id`, `festival_id -> festivals.id`, `checkin_id -> festival_checkins.id` |
| 상태값 | `match_pools.status` |
| CHECK | `preferred_group_size IN (2,3,4)`, `search_expires_at > entered_at`, `status IN (...)` |
| UNIQUE | active 상태의 `member_id` partial unique index 후보 |
| INDEX | `idx_match_pools_festival_status_expires`, `idx_match_pools_member_status`, `idx_match_pools_locked_at` |
| 개인정보/보안 | 태그는 민감정보가 되지 않도록 선택지를 제한한다. 자유 텍스트 조건은 두지 않는다. |
| MVP 필수 | 필수 |

### match_attempts

| 항목 | 내용 |
| --- | --- |
| 목적 | Scheduler 또는 신규 진입 트리거가 만든 후보 그룹 시도를 저장한다. |
| 주요 컬럼 | `id`, `festival_id`, `target_group_size`, `status`, `score`, `created_by`, `started_at`, `expires_at`, `confirmed_at`, `failed_reason`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `festival_id -> festivals.id` |
| 상태값 | `match_attempts.status` |
| CHECK | `target_group_size IN (2,3,4)`, `expires_at > started_at`, `status IN (...)` |
| UNIQUE | 없음 |
| INDEX | `idx_match_attempts_festival_status`, `idx_match_attempts_expires_at`, `idx_match_attempts_status_created_at` |
| 개인정보/보안 | 개인 식별은 member 테이블 FK를 통해서만 연결한다. |
| MVP 필수 | 필수 |

### match_attempt_members

| 항목 | 내용 |
| --- | --- |
| 목적 | 하나의 매칭 시도에 포함된 후보 사용자와 pool entry를 연결한다. |
| 주요 컬럼 | `id`, `attempt_id`, `member_id`, `pool_id`, `member_score`, `status`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `attempt_id -> match_attempts.id`, `member_id -> members.id`, `pool_id -> match_pools.id` |
| 상태값 | `CANDIDATE`, `PROPOSED`, `ACCEPTED`, `REJECTED`, `TIMEOUT`, `EXCLUDED` |
| CHECK | `status IN (...)`, `member_score >= 0` |
| UNIQUE | `(attempt_id, member_id)`, `(attempt_id, pool_id)` |
| INDEX | `idx_match_attempt_members_member`, `idx_match_attempt_members_attempt_status` |
| 개인정보/보안 | 후보 선정 감사용이며 자유 입력을 저장하지 않는다. |
| MVP 필수 | 필수 |

### match_proposals

| 항목 | 내용 |
| --- | --- |
| 목적 | 각 후보 사용자에게 보낸 30초 매칭 제안 상태를 저장한다. |
| 주요 컬럼 | `id`, `attempt_id`, `member_id`, `status`, `sent_at`, `expires_at`, `responded_at`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `attempt_id -> match_attempts.id`, `member_id -> members.id` |
| 상태값 | `match_proposals.status` |
| CHECK | `expires_at > sent_at`, `responded_at IS NULL OR responded_at >= sent_at`, `status IN (...)` |
| UNIQUE | `(attempt_id, member_id)` |
| INDEX | `idx_match_proposals_member_status`, `idx_match_proposals_expires_status`, `idx_match_proposals_attempt_status` |
| 개인정보/보안 | 제안 자체에는 메시지 본문을 저장하지 않는다. |
| MVP 필수 | 필수 |

### match_responses

| 항목 | 내용 |
| --- | --- |
| 목적 | 제안에 대한 사용자 응답을 중복 없이 저장한다. |
| 주요 컬럼 | `id`, `proposal_id`, `attempt_id`, `member_id`, `response`, `responded_at`, `created_at` |
| PK | `id` |
| FK | `proposal_id -> match_proposals.id`, `attempt_id -> match_attempts.id`, `member_id -> members.id` |
| 상태값 | `match_responses.response` |
| CHECK | `response IN (...)` |
| UNIQUE | `(proposal_id, member_id)` |
| INDEX | `idx_match_responses_attempt_response`, `idx_match_responses_member_created_at` |
| 개인정보/보안 | 버튼형 응답만 저장한다. 자유 텍스트 응답은 없다. |
| MVP 필수 | 필수 |

### match_groups

| 항목 | 내용 |
| --- | --- |
| 목적 | 최소 2명 이상 수락으로 확정된 매칭 그룹을 저장한다. |
| 주요 컬럼 | `id`, `attempt_id`, `festival_id`, `status`, `confirmed_member_count`, `meeting_place_name`, `meeting_place_content_id`, `meeting_map_x`, `meeting_map_y`, `confirmed_at`, `started_at`, `completed_at`, `cancelled_at`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `attempt_id -> match_attempts.id`, `festival_id -> festivals.id` |
| 상태값 | `match_groups.status` |
| CHECK | `confirmed_member_count BETWEEN 2 AND 4`, `status IN (...)` |
| UNIQUE | `attempt_id` |
| INDEX | `idx_match_groups_festival_status`, `idx_match_groups_status_confirmed_at` |
| 개인정보/보안 | 만남 포인트는 축제 공식 좌표 또는 공공 장소 기준으로 저장한다. 사용자 실시간 위치는 저장하지 않는다. |
| MVP 필수 | 필수 |

### match_group_members

| 항목 | 내용 |
| --- | --- |
| 목적 | 확정 그룹의 참여자 상태, 도착 예정 시간, 도착 인증 상태를 저장한다. |
| 주요 컬럼 | `id`, `group_id`, `member_id`, `status`, `arrival_minutes`, `arrival_time_selected_at`, `arrived_at`, `cancelled_at`, `cancel_reason`, `created_at`, `updated_at` |
| PK | `id` |
| FK | `group_id -> match_groups.id`, `member_id -> members.id` |
| 상태값 | `JOINED`, `ARRIVAL_TIME_SELECTED`, `ARRIVED`, `CANCELLED`, `NO_SHOW`, `LEFT` |
| CHECK | `arrival_minutes IN (0,5,10,20,30)`, `status IN (...)` |
| UNIQUE | `(group_id, member_id)`, active 상태의 `member_id` partial unique index 후보 |
| INDEX | `idx_match_group_members_member_status`, `idx_match_group_members_group_status` |
| 개인정보/보안 | 취소 사유는 구조화된 버튼 값만 저장한다. |
| MVP 필수 | 필수 |

### match_events

| 항목 | 내용 |
| --- | --- |
| 목적 | 매칭, 제안, 응답, 도착, 취소, 안전 리마인드 등 상태 변화를 append-only로 기록한다. |
| 주요 컬럼 | `id`, `group_id`, `attempt_id`, `member_id`, `event_type`, `payload`, `created_at` |
| PK | `id` |
| FK | `group_id -> match_groups.id`, `attempt_id -> match_attempts.id`, `member_id -> members.id` |
| 상태값 | `event_type` |
| CHECK | `event_type IN ('MATCH_PROPOSED','MATCH_ACCEPTED','MATCH_REJECTED','MATCH_TIMEOUT','MATCH_INSUFFICIENT_MEMBERS','MATCH_CONFIRMED','ARRIVAL_TIME_SELECTED','MEMBER_ARRIVED','MEMBER_CANCELLED','MATCH_CANCELLED','SAFETY_REMINDER')` |
| UNIQUE | 없음 |
| INDEX | `idx_match_events_group_created_at`, `idx_match_events_attempt_created_at`, `idx_match_events_member_created_at`, `idx_match_events_type_created_at` |
| 개인정보/보안 | `payload JSONB`에는 token, GPS 원본 좌표, 민감정보를 저장하지 않는다. |
| MVP 필수 | 필수 |

### match_cooldowns

| 항목 | 내용 |
| --- | --- |
| 목적 | 거절/미응답/취소 후 재매칭 제한 시간을 관리한다. |
| 주요 컬럼 | `id`, `member_id`, `reason`, `starts_at`, `expires_at`, `created_at` |
| PK | `id` |
| FK | `member_id -> members.id` |
| 상태값 | `reason` |
| CHECK | `expires_at > starts_at`, `reason IN ('REJECT','TIMEOUT','CANCEL','NO_SHOW','REPORT')` |
| UNIQUE | active 상태의 `member_id` partial unique index 후보 |
| INDEX | `idx_match_cooldowns_member_expires`, `idx_match_cooldowns_expires_at` |
| 개인정보/보안 | 사유는 구조화된 코드만 저장한다. |
| MVP 필수 | 필수 |

### match_penalty_events

| 항목 | 내용 |
| --- | --- |
| 목적 | 패널티 점수 증감 이력을 저장하고 자정/2시간 감소 정책의 근거로 사용한다. |
| 주요 컬럼 | `id`, `member_id`, `event_type`, `score_delta`, `reason`, `related_group_id`, `related_attempt_id`, `created_at` |
| PK | `id` |
| FK | `member_id -> members.id`, `related_group_id -> match_groups.id`, `related_attempt_id -> match_attempts.id` |
| 상태값 | `event_type` |
| CHECK | `event_type IN ('TIMEOUT','CANCEL','NO_SHOW','REPORT_CONFIRMED','DECAY','ADMIN_ADJUST')` |
| UNIQUE | 없음 |
| INDEX | `idx_match_penalty_events_member_created_at`, `idx_match_penalty_events_type_created_at` |
| 개인정보/보안 | 운영 이력은 30일 후 삭제 또는 집계 전환 후보로 둔다. |
| MVP 필수 | 필수 |

### member_reviews

| 항목 | 내용 |
| --- | --- |
| 목적 | 동행 후 평가와 매너온도 변화를 기록한다. |
| 주요 컬럼 | `id`, `group_id`, `reviewer_member_id`, `reviewee_member_id`, `rating`, `manner_delta`, `review_tags`, `comment_encrypted`, `created_at` |
| PK | `id` |
| FK | `group_id -> match_groups.id`, `reviewer_member_id -> members.id`, `reviewee_member_id -> members.id` |
| 상태값 | 없음 |
| CHECK | `rating BETWEEN 1 AND 5`, `reviewer_member_id <> reviewee_member_id` |
| UNIQUE | `(group_id, reviewer_member_id, reviewee_member_id)` |
| INDEX | `idx_member_reviews_reviewee_created_at`, `idx_member_reviews_group` |
| 개인정보/보안 | 한줄 평을 허용하면 암호화 저장 후보로 둔다. MVP에서는 태그형 평가를 우선한다. |
| MVP 필수 | 필수 |

### reports

| 항목 | 내용 |
| --- | --- |
| 목적 | 구조화된 신고를 접수하고 관리자 처리 상태를 관리한다. |
| 주요 컬럼 | `id`, `reporter_member_id`, `reported_member_id`, `group_id`, `reason_code`, `detail_encrypted`, `status`, `created_at`, `updated_at`, `resolved_at` |
| PK | `id` |
| FK | `reporter_member_id -> members.id`, `reported_member_id -> members.id`, `group_id -> match_groups.id` |
| 상태값 | `reports.status`, `reason_code` |
| CHECK | `reporter_member_id <> reported_member_id`, `status IN (...)`, `reason_code IN ('RUDE','SEXUAL_HARASSMENT','NO_SHOW','SCAM','SAFETY','OTHER')` |
| UNIQUE | 중복 신고 방지를 위해 `(reporter_member_id, reported_member_id, group_id, reason_code)` 후보 |
| INDEX | `idx_reports_status_created_at`, `idx_reports_reported_member`, `idx_reports_reporter_member` |
| 개인정보/보안 | 상세 내용은 암호화 저장 후보다. 신고 내용은 피신고자 개인정보를 포함할 수 있다. |
| MVP 필수 | 필수 |

### admin_actions

| 항목 | 내용 |
| --- | --- |
| 목적 | 신고 처리, 제재, 수동 패널티, 데이터 보정 등 관리자 조치 이력을 남긴다. |
| 주요 컬럼 | `id`, `admin_member_id`, `target_member_id`, `report_id`, `action_type`, `reason`, `metadata`, `created_at` |
| PK | `id` |
| FK | `admin_member_id -> members.id`, `target_member_id -> members.id`, `report_id -> reports.id` |
| 상태값 | `admin_actions.action_type` |
| CHECK | `action_type IN (...)` |
| UNIQUE | 없음 |
| INDEX | `idx_admin_actions_admin_created_at`, `idx_admin_actions_target_created_at`, `idx_admin_actions_report` |
| 개인정보/보안 | 관리자 사유와 metadata에는 Secret, token, GPS 원본 좌표를 저장하지 않는다. |
| MVP 필수 | 필수 |

### solo_courses

| 항목 | 내용 |
| --- | --- |
| 목적 | 매칭 실패 후 제공하는 솔로 45분 코스 추천 결과를 저장한다. |
| 주요 컬럼 | `id`, `member_id`, `festival_id`, `source_attempt_id`, `title`, `status`, `created_at`, `expires_at` |
| PK | `id` |
| FK | `member_id -> members.id`, `festival_id -> festivals.id`, `source_attempt_id -> match_attempts.id` |
| 상태값 | `ACTIVE`, `EXPIRED`, `DISMISSED` |
| CHECK | `expires_at > created_at`, `status IN (...)` |
| UNIQUE | 없음 |
| INDEX | `idx_solo_courses_member_status`, `idx_solo_courses_festival_created_at` |
| 개인정보/보안 | 추천 결과는 서비스 사용 이력이다. 과도한 장기 보관을 피한다. |
| MVP 필수 | 필수 |

### solo_course_places

| 항목 | 내용 |
| --- | --- |
| 목적 | 솔로 코스에 포함된 장소 순서와 추천 근거를 저장한다. |
| 주요 컬럼 | `id`, `solo_course_id`, `tour_place_id`, `display_order`, `recommendation_reason`, `created_at` |
| PK | `id` |
| FK | `solo_course_id -> solo_courses.id`, `tour_place_id -> tour_places.id` |
| 상태값 | 없음 |
| CHECK | `display_order > 0` |
| UNIQUE | `(solo_course_id, display_order)`, `(solo_course_id, tour_place_id)` |
| INDEX | `idx_solo_course_places_course_order` |
| 개인정보/보안 | 추천 근거는 구조화된 짧은 코드 또는 문구로 제한한다. |
| MVP 필수 | 필수 |

### recommendation_click_logs

| 항목 | 내용 |
| --- | --- |
| 목적 | 추천 장소와 솔로 코스 클릭률 KPI를 측정한다. |
| 주요 컬럼 | `id`, `member_id`, `festival_id`, `tour_place_id`, `solo_course_id`, `source`, `clicked_at`, `created_at` |
| PK | `id` |
| FK | `member_id -> members.id`, `festival_id -> festivals.id`, `tour_place_id -> tour_places.id`, `solo_course_id -> solo_courses.id` |
| 상태값 | `source` |
| CHECK | `source IN ('MATCH_FAILED','MATCH_ROOM','SOLO_COURSE','FESTIVAL_DETAIL')` |
| UNIQUE | 없음 |
| INDEX | `idx_recommendation_click_logs_member_clicked_at`, `idx_recommendation_click_logs_festival_clicked_at`, `idx_recommendation_click_logs_source_clicked_at` |
| 개인정보/보안 | 행동 로그는 분석 목적의 최소 필드만 저장하고 장기 보관 정책을 별도로 둔다. |
| MVP 필수 | 필수 |

## 7. 매칭 흐름과 DB 표현

1. 사용자가 축제 상세 또는 체크인 화면에서 GPS 검증을 통과한다.
2. 서버는 원본 좌표를 저장하지 않고 `festival_checkins`에 성공 metadata를 저장한다.
3. 사용자가 희망 인원, 태그, `allow_minimum_two`를 선택하면 `match_pools`에 `WAITING` row를 만든다.
4. `match_pools`는 active 상태의 사용자 중복 진입을 partial unique index로 막는다.
5. Scheduler는 같은 `festival_id`, `WAITING`, `search_expires_at > now()` 조건으로 후보를 조회한다.
6. 후보 조회는 구현 단계에서 `SELECT ... FOR UPDATE SKIP LOCKED`를 사용한다.
7. 차단 관계는 `user_blocks`에서 양방향으로 제외한다.
8. 후보가 정해지면 `match_attempts`, `match_attempt_members`, `match_proposals`를 생성한다.
9. 각 proposal은 `sent_at`, `expires_at`으로 30초 응답 제한을 표현한다.
10. 사용자의 수락/거절/타임아웃은 `match_responses`와 `match_proposals.status`로 저장한다.
11. 응답 중복은 `(proposal_id, member_id)` unique constraint로 막는다.
12. 최소 2명 이상 수락하고 정책을 만족하면 `match_groups`, `match_group_members`를 생성한다.
13. active group 중복 참여는 `match_group_members.member_id` partial unique index 후보로 막는다.
14. 상태 변경은 `match_events`에 append-only로 기록한다.
15. 거절, 타임아웃, 확정 후 취소, 노쇼는 `match_penalty_events`와 `match_cooldowns`에 기록한다.

## 8. 주요 제약조건과 인덱스 요약

### 주요 CHECK constraint 후보

- `members.provider IN ('KAKAO')`
- `members.role IN ('USER','ADMIN')`
- `members.manner_temperature BETWEEN 0 AND 100`
- `match_pools.preferred_group_size IN (2,3,4)`
- `match_pools.search_expires_at > match_pools.entered_at`
- `match_attempts.target_group_size IN (2,3,4)`
- `match_proposals.expires_at > match_proposals.sent_at`
- `match_groups.confirmed_member_count BETWEEN 2 AND 4`
- `match_group_members.arrival_minutes IN (0,5,10,20,30)`
- `reports.reporter_member_id <> reports.reported_member_id`
- `festival_checkins.distance_meters >= 0`

### 주요 UNIQUE constraint 후보

- `members(provider, provider_user_id)`
- `refresh_tokens(token_hash)`
- `festivals(content_id)`
- `tour_places(content_id)`
- `festival_images(festival_id, origin_image_url)`
- `user_blocks(blocker_member_id, blocked_member_id)`
- `match_attempt_members(attempt_id, member_id)`
- `match_proposals(attempt_id, member_id)`
- `match_responses(proposal_id, member_id)`
- `match_groups(attempt_id)`
- `match_group_members(group_id, member_id)`
- `member_reviews(group_id, reviewer_member_id, reviewee_member_id)`

### 주요 partial unique index 후보

PostgreSQL migration 작성 시 partial unique index로 표현한다.

- active 상태의 `festival_checkins(member_id, festival_id)`
- active 상태의 `match_pools(member_id)`
- active 상태의 `match_group_members(member_id)`
- active 상태의 `match_cooldowns(member_id)`

### 주요 조회 인덱스 후보

- `match_pools(festival_id, status, search_expires_at)`
- `match_proposals(expires_at, status)`
- `match_attempts(festival_id, status)`
- `match_events(group_id, created_at)`
- `user_blocks(blocker_member_id)`
- `user_blocks(blocked_member_id)`
- `festival_checkins(festival_id, status, expires_at)`
- `reports(status, created_at)`
- `tour_api_call_logs(operation_name, called_at)`
- `recommendation_click_logs(source, clicked_at)`

## 9. 개인정보와 보안 고려사항

- 원본 GPS 좌표는 DB에 저장하지 않는다.
- `festival_checkins`는 체크인 성공 결과, 거리, 만료 시각만 저장한다.
- 성별/연령대는 `BYTEA` 암호화 저장 후보로 둔다.
- 신고 상세 내용과 리뷰 자유 문구는 암호화 저장 후보로 둔다.
- Refresh Token은 원문이 아니라 hash만 저장한다.
- API Key, OAuth secret, DB password, SSH Key는 DB와 문서에 저장하지 않는다.
- `match_events.payload`, `admin_actions.metadata`, `tour_api_call_logs`에는 Secret과 원본 GPS 좌표를 넣지 않는다.
- 탈퇴 시 `members`의 개인정보 컬럼은 즉시 익명화하고 상태를 `WITHDRAWN` 또는 `DELETED`로 변경한다.
- 패널티/매칭 이벤트 등 운영 로그는 30일 보관 후 삭제 또는 집계 전환 정책을 별도 구현한다.

## 10. 9-2단계 SQL 생성 대상

다음 단계에서 생성할 Flyway SQL 파일 후보:

```text
db/migration/V2__create_core_tables.sql
db/migration/V3__create_matching_tables.sql
db/migration/V4__create_safety_admin_recommendation_tables.sql
```

9-2단계에서는 이 문서를 기준으로 실제 DDL, FK, CHECK constraint, UNIQUE constraint, index를 작성한다.

`V1__init.sql`은 이미 적용된 migration이므로 수정하지 않는다.
