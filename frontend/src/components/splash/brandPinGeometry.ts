/**
 * meet·or·solo 핀 심볼의 좌표.
 *
 * <p>스플래시의 {@link BrandPin}(애니메이션 SVG)과 `public/icons/icon.svg`(favicon·PWA 아이콘)가
 * <b>같은 도형</b>을 그린다. 한쪽만 고치면 탭 아이콘과 앱 첫 화면의 로고가 달라지므로, 좌표를
 * 여기 모아 두고 `brandPinIcon.test.ts`가 두 곳이 같은지 확인한다.
 *
 * <p>정적 아이콘 파일은 `public/`에 있어 이 모듈을 import할 수 없다. 그래서 값을 공유하는
 * 대신 <b>테스트가 파일 내용을 읽어 대조</b>한다.
 *
 * <p>좌표는 원본 로고 PNG(`frontend/logo.png`)를 격자로 스캔해 옮긴 값이다. 100x112 viewBox에서
 * 안쪽 흰 원은 (50,44) r27.5, 외곽 물방울은 반지름 38 원의 상단을 크게 돌아(`large-arc`)
 * 아래 꼭짓점 (50,99)으로 접선 연결한 경로다.
 */

/** 외곽 물방울. 위쪽 원을 크게 돌아 아래 꼭짓점으로 모인다. */
export const PIN_OUTLINE = 'M22.5 70.3 A38 38 0 1 1 77.5 70.3 L50 99 Z';

/** 안쪽 흰 원. 두 사람을 clip하는 경계이기도 하다. */
export const INNER_CIRCLE = { cx: 50, cy: 44, r: 27.5 };

/**
 * 한 사람의 몸통과 치켜든 팔. 팔은 손끝으로 갈수록 좁아지는 쐐기이고, 어깨는 원 왼쪽 아래를
 * 채우며 흘러내린다. 아래로 길게 뻗은 구간은 안쪽 원 clip이 잘라낸다.
 */
export const FIGURE_BODY = [
  'M48 35.2',
  'C46.9 37.3 45.4 40 44 42.6',
  'C43 44.8 41.6 46.6 39.6 47.6',
  'C35.4 49.6 30 50.6 26 53.2',
  'C22.5 55.4 19 58.4 16 61',
  'L16 78',
  'L39.4 78',
  'C38.4 71 37.8 65 38.6 60.4',
  'C39.4 56.4 41.8 54.4 44.8 52.6',
  'C47.2 51 48.6 46.6 49.6 42.2',
  'C50.2 38.6 49.8 36.2 48 35.2',
  'Z',
].join(' ');

export const HEAD = { cx: 30.8, cy: 42.1, r: 6.3 };

/** 맞닿은 손 위로 튀는 하이파이브 광선 3줄. 스플래시에서는 60ms씩 늦게 팝한다. */
export const RAYS = [
  { d: 'M50 23 L50 29', originX: 50, originY: 26 },
  { d: 'M40.4 27.6 L44.1 31.5', originX: 42.25, originY: 29.55 },
  { d: 'M59.6 27.6 L55.9 31.5', originX: 57.75, originY: 29.55 },
] as const;

export const RAY_STROKE_WIDTH = 3.2;

/**
 * 핀이 착지하며 옆으로 퍼지는 바닥 타원.
 *
 * <p><b>정적 아이콘에는 넣지 않는다.</b> 착지 동작을 설명하는 조각이라 멈춰 있으면 의미가
 * 없고, 16px 탭 아이콘에서는 핀 아래 얼룩으로만 보인다.
 */
export const GROUND_SHADOW = { cx: 50, cy: 103.5, rx: 21.3, ry: 4 };
